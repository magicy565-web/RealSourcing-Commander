// knowledgeImport.ts — 知识库导入存根
export async function parseExcel(filePath: string): Promise<unknown[]> {
  console.log("[KnowledgeImport] parseExcel stub:", filePath);
  return [];
}
export async function parseWord(filePath: string): Promise<string> {
  console.log("[KnowledgeImport] parseWord stub:", filePath);
  return "";
}
